// const cors = require('cors');

// let corsOptions = {
//     origin: 'http://localhost:3000',
//     optionsSuccessStatus: 200
// };

// const corsHandler = () => async (appI, next) => {
//     appI.use(cors(corsOptions))
//     await next()
// };

// module.exports = corsHandler;
