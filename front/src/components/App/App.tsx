import React, { FC } from 'react';
import MainWrapper from '../common/MainWrapper/MainWrapper';

const App: FC = () => {
  return (
    <MainWrapper>
      
    </MainWrapper>
  );
};

export default App;